---
trigger: always_on
---

Rule: For the Go backend, use the 'Gin' framework for speed and 'Standard Layout' (cmd/internal/pkg). Ensure every endpoint includes a JSON structured response for the Next.js frontend. If the agent gets stuck on a Go dependency, use '// turbo go mod tidy' immediately.