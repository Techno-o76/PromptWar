---
trigger: always_on
---

Rule: Keep responses concise. Use '// turbo' for repetitive terminal tasks. When refactoring, only output the changed code blocks, not the whole file, to save tokens and prevent "AI drift".